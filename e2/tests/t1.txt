int ab2, ad[123];

int fun1(const char a, int b) {}

char fun2(const char a, int b, string a) {
    static const int a, b, c, d <= 10.5e-10, e, f <= b;
    static char g;
    const float h;
    bool i;

    id[1+2] << 1;
    // id[i*b] << -1;
    // id << -1;
    id << 1;
    id[i&b] << 1;
    // id[i || b] << 1;
    id[a ? b : c] << 1;
}

char fun4() {
    output 2;
    input id;
    break;
    continue;
}

char fun5() {
     if(i * e) {
         if(i > 0) {
             continue;
         } else {
            while ( i | !(0 && true || false)) do {
                return 0;
            };
         };
     };
}